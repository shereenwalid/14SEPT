_pipeline_state/ — Internal pipeline state. Holds the processing ledger (what has been parsed and at which version), the run lock that prevents concurrent executions, and the per-opportunity completeness report. Not parsed output and not source data: these objects are written and read only by the pipeline, and deleting the ledger causes a full re-ingestion of every document.
